// Setup file for Jest — runs after the test framework is installed in the environment.
// Add global mocks, testing-library setup, or custom matchers here.

// Example: import '@testing-library/jest-dom/extend-expect';
import '@testing-library/jest-dom';

export {};
